import { style } from '@vanilla-extract/css';

export const originBlogView = style({
    backgroundColor: '#0080FE',
    display: 'flex',
    width: '100%',
    height: '100%',
    // minHeight: '100vh'
});     